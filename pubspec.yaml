name: my_services
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: ">=2.17.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:

  cupertino_icons: ^1.0.8

  google_fonts: ^6.2.1

  firebase_core: ^3.2.0 #firebaseCore

  firebase_messaging: ^15.0.3 #firebaseMessaging

  firebase_crashlytics: ^4.0.3 #firebaseCrashlytics
  
  firebase_auth: ^5.1.2 #firebaseAuth

  app_settings: ^5.1.1 #appSettings

  google_maps_flutter: ^2.7.0 #googleMaps

  google_maps_cluster_manager: ^3.1.0 #googleMaps

  image_cropper: ^7.1.0 #imagePicker

  image_picker: ^1.1.2 #imagePicker

  flutter_image_compress: ^2.3.0 #imagePicker

  photo_view: ^0.15.0 #photoView

  smooth_page_indicator: ^1.2.0 #photoView

  map_launcher: ^3.3.1 #mapLauncherff

  flutter_svg: ^2.0.10+1 #flutterSvg

  app_links: ^6.1.4 #appLinks

  share_plus: ^9.0.0 #sharePlus

  pinput: ^5.0.0 #pinCodeFields

  hive: ^2.2.3 #hive

  hive_flutter: ^1.1.0 #hive

  sqflite: ^2.3.3+1 #sqflite

  flutter_app_badger: ^1.5.0 #flutterAppBadger

  location: ^7.0.0 #location

  logger: ^2.3.0

  url_launcher: ^6.3.0

  intl: ^0.19.0

  device_info_plus: ^10.1.0

  package_info_plus: ^8.0.0

  path_provider: ^2.1.3

  timeago: ^3.7.0

  octo_image: ^2.0.0

  flutter_blurhash: ^0.8.2
  
  flutter_image: ^4.1.11

  cached_network_image: ^3.3.1

  flutter_riverpod: ^2.5.1

  freezed_annotation: ^2.4.3

  json_annotation: ^4.9.0

  dio: ^5.5.0+1 #dio
  
  get_it: ^7.7.0

  mdi:
    git:
      url: https://github.com/SalehHub/mdi-dart.git
      path: mdi

  flutter_sheet_localization: #^5.1.1
      git:
        url: https://github.com/SalehHub/flutter_sheet_localization.git
        path: flutter_sheet_localization
  
  
  flutter:
    sdk: flutter

  
  flutter_localizations:
    sdk: flutter

  http: any
  # flutter_test: any
dev_dependencies:
  build_runner: ^2.4.11

  freezed: ^2.5.2

  json_serializable: ^6.8.0

  flutter_lints: ^4.0.0
  flutter_sheet_localization_generator: #^5.1.7
      git:
        url: https://github.com/SalehHub/flutter_sheet_localization.git
        path: flutter_sheet_localization_generator

#override dependeinces
dependency_overrides:
  http: ^1.2.1

flutter:
  assets:
    - flags/
    - google_fonts/